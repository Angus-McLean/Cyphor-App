

class CyphorInput {
	constructor() {
		this.events = new EventEmitter();
		this.button;
		this.input;
	}
}

CyphorInput.create = function (elemsObj, channelObj) {
	// instatiate new CyphorInput
	
	// create InputOverlay
	
	// Add listeners to recipientElem
	
	// Add ButtonOverlay if required
	
	// setUpMessageListeners
	
};

function setUpMessageListeners(messageClient, inputObj) {
	// :send_text
	// :configure_button
	// :change
	// :deleted
}